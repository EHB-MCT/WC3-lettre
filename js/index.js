"use strict";

import wordString from "./../assets/wordlist.js";

// to google
// chop string to 5 characters      (splice)
// string to array js               (split)


const wordl = {
    wordToGuess: [],
    guessedWord: [],
    init() {
        this.reset()

    },
    pickRandomWord() {
    },

    matchWord: () => {
    },

    addGuess: () => {
    },
    showSolution() {
    },
    reset() {
    }
}

wordl.init();